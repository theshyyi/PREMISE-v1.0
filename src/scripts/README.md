# Script notes

Scripts in this folder provide CLI-style entry points and examples for common workflows.

Some scripts originated as project-specific research utilities. For publication-oriented reuse, prefer the examples under the top-level `examples/` directory and update scripts to read paths from arguments or configuration files.
