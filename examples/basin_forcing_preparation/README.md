# Basin forcing preparation example

Run:

```bash
python examples/basin_forcing_preparation/run_synthetic_basin_preprocessing.py
```

This synthetic example demonstrates variable renaming, temporal subsetting, bounding-box clipping, and basin-mean aggregation without requiring external shapefiles.
